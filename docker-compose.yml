version: '3.8'

services:
  redis:
    image: redis:latest
    ports:
      - 6379:6379
    networks:
      - redis-pubsub-tutorial
  redis-insight:
    image: redis/redisinsight:latest
    ports:
      - 5540:5540
    networks:
      - redis-pubsub-tutorial

networks:
  redis-pubsub-tutorial: